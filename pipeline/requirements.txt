# SuperHSSG Pipeline 의존성
# Python 3.10 ~ 3.12 권장

# 수치 연산
numpy
scipy
scikit-learn
scikit-image

# 이미지 처리
opencv-python
Pillow
matplotlib

# 딥러닝 (CUDA 버전은 아래 PyTorch 설치 가이드 참고)
torch
torchvision

# SAM (Segment Anything Model)
# 체크포인트 다운로드 (macOS):
#   curl -L -o pipeline/sam_vit_h_4b8939.pth https://dl.fbaipublicfiles.com/segment_anything/sam_vit_h_4b8939.pth
# 체크포인트 다운로드 (Linux):
#   wget https://dl.fbaipublicfiles.com/segment_anything/sam_vit_h_4b8939.pth -P pipeline/
segment-anything

# OpenShape / CLIP (Stage 2)
open-clip-torch
huggingface-hub

# 시각화 (선택, 없어도 파이프라인 실행 가능)
open3d
